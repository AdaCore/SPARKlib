with "sparklib_light";
project Test is

   for Main use ("main.adb");
   for Runtime ("ada") use "light";

   package Builder is
      for Global_Configuration_Pragmas use "pragmas.adc";
   end Builder;

   package Compiler is
      for Default_Switches ("Ada") use ("-gnata", "-gnat2022", "-gnateDSPARK_BODY_MODE=Off");
   end Compiler;

end Test;
