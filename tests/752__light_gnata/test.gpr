with "sparklib_light";
project Test is

   for Main use ("main.adb");
   for Runtime ("ada") use "light";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnata", "-gnat2022");
   end Compiler;

end Test;
